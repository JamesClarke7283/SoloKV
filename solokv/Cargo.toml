[package]
name = "solokv"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
readme = "README.md"

[dependencies]
clap = { version = "4.5.0", features = ["derive"] }
libsolokv = { path = "../libsolokv" }

[lints]
workspace = true
